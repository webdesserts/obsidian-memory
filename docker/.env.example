# Required: Path to your Obsidian vault on the host
NOTES_PATH=/path/to/your/notes

# Domain for TLS certificate (default: leda.webdesserts.com)
DOMAIN=leda.webdesserts.com

# Optional: Path to obsidian-p2p-sync repo if not at default location
# SYNC_DAEMON_PATH=../../obsidian-p2p-sync
