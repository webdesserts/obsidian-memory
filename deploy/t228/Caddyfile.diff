--- a/Caddyfile (live, /opt/docker/caddy/Caddyfile)
+++ b/Caddyfile.staged (deploy/t228/Caddyfile.staged)
@@ -1,56 +1,33 @@
-# STAGED Caddyfile — NOT ACTIVE. Written by worker-t237, obsidian-memory
-# tip 2799c8d (2 commits: retired auth-flow surface removal from
-# auth-service @5d83264, passkey-login pending-field cut @2799c8d) per
-# autonomy/t:237; the Worker builds and stages only, the orchestrator
-# performs every live mutation.
+# Live Caddyfile. Last change: autonomy/t:228, 2026-09-02. Rollback: deploy/t228/ROLLBACK.md
 #
-# NOTE ON THIS HEADER: deliberately avoids the literal string t:237
-# retires (see the crate commit messages at the SHAs above for the full
-# name), since criterion 1's check is a case-insensitive grep for that
-# string returning zero matches against the live Caddyfile — and this
-# header becomes part of that file verbatim once activated (same
-# copy-in-place convention as the existing docker-compose.override.yml.STAGED,
-# whose header is byte-identical to its live counterpart today). Every
-# removed route is instead described by its Caddy purpose below, not its
-# literal path text or the retired branch/file name.
+# Written by worker-t228, obsidian-memory branch t228 (commits d264c2d,
+# b74b714) per autonomy/t:228; the Worker builds and stages only, the
+# orchestrator performs every live mutation.
 #
-# WHAT THIS DOES: removes the 5 retired-auth-flow `handle` blocks that
-# reverse-proxied to auth-service's now-deleted RFC 8414 discovery,
-# RFC 7591 dynamic client registration, authorize, and token endpoints:
-#   - the root-level discovery block (RFC 8414 metadata at /.well-known/...)
-#   - the legacy /auth-prefixed discovery block (same metadata, /auth path)
-#   - the /auth/register block (dynamic client registration)
-#   - the /auth/authorize* block (authorization endpoint)
-#   - the /auth/token block (token endpoint)
-# Also rewords the /mcp* block's comment (it named the retired bearer-token
-# credential type auth-service no longer accepts) to just "requires a
-# valid API key". Everything else — passkey setup/login/logout routes,
-# /validate forward_auth, /mcp*, /llm/*, the relay/livekit/voice/design/sync
-# routes, and the catch-all prime-agent proxy — is untouched.
+# WHAT THIS DOES: adds X-Auth-Actor to the copy_headers list on all four
+# forward_auth blocks (/mcp*, /llm/*, /design/*, catch-all). X-Auth-User
+# behavior is unchanged on all four -- this is purely additive. The
+# auth-service code change (branch t228, crates/auth-service/src/validation.rs)
+# emits X-Auth-Actor conditionally: only when the winning credential
+# carries a known actor uuid (today: session-cookie logins, via
+# StoredUser.id; API keys have none until autonomy/t:227), never with an
+# empty/placeholder value. Verified 2026-09-02: neither the memory crate
+# nor llama-swap reads any X-Auth header today, so this edit alone is
+# inert on /mcp* and /llm* until a consumer is built. This Caddyfile edit
+# alone does nothing until the auth-service image running in production
+# actually emits the header -- see deploy/t228/DEPLOY.md for the required
+# accompanying auth-service deploy step.
 #
-# POST-REMOVAL BEHAVIOR for the freed path prefixes: Caddy's `handle`
-# blocks are mutually exclusive per server block, so a request to e.g.
-# /auth/register now falls through to the catch-all `handle {}` (the
-# forward_auth-gated prime-agent proxy). Unauthenticated, that's a 401 ->
-# `redir * /auth/login 302` — the correct, secure post-removal outcome
-# (previously-public endpoints become auth-gated), not a bare 404.
-# Deploy-time probes should expect a 302-to-login here, not treat it as a
-# failure.
-#
-# TO ACTIVATE (orchestrator-executed; run from /opt/docker/caddy — this
-# staged file is the one sibling matching `ls Caddyfile.staged-t237-*`
-# in this same directory, deliberately not spelled out here per the note
-# above):
+# TO ACTIVATE (orchestrator-executed; run from /opt/docker/caddy):
 #   md5 -q Caddyfile   # record before-state
-#   cp Caddyfile Caddyfile.bak-2026-08-27-t237-caddy
-#   cp Caddyfile.staged-t237-* Caddyfile
+#   cp Caddyfile Caddyfile.bak-2026-09-02-t228-caddy
+#   cp <worktree>/deploy/t228/Caddyfile.staged Caddyfile
 #   docker restart caddy-caddy-1   # prefer over `caddy reload` per Device.md's bind-mount footgun
 #   md5 -q Caddyfile   # must equal:
 #   docker exec caddy-caddy-1 md5sum /etc/caddy/Caddyfile   # (virtiofs stale-view check)
 #
-# ROLLBACK: restore the dated backup and restart:
-#   cp /opt/docker/caddy/Caddyfile.bak-2026-08-27-t237-caddy /opt/docker/caddy/Caddyfile
-#   docker restart caddy-caddy-1
+# ROLLBACK: restore the dated backup and restart. Full recipe in
+# deploy/t228/ROLLBACK.md.
 # No volumes are touched by either path.
 
 # Obsidian Memory LAN Stack
@@ -88,7 +65,7 @@
     handle /mcp* {
         forward_auth auth-service:3001 {
             uri /validate
-            copy_headers Authorization
+            copy_headers Authorization X-Auth-Actor
         }
         reverse_proxy memory:3000 {
             # Critical: disable buffering for SSE streaming
@@ -113,7 +90,7 @@
     handle /llm/* {
         forward_auth auth-service:3001 {
             uri /validate
-            copy_headers Authorization
+            copy_headers Authorization X-Auth-Actor
         }
         uri strip_prefix /llm
         reverse_proxy host.docker.internal:8081
@@ -156,7 +133,7 @@
     handle_path /design/* {
         forward_auth auth-service:3001 {
             uri /validate
-            copy_headers X-Auth-User
+            copy_headers X-Auth-User X-Auth-Actor
             @denied status 401 403
             handle_response @denied {
                 redir * /auth/login 302
@@ -171,7 +148,7 @@
     handle {
         forward_auth auth-service:3001 {
             uri /validate
-            copy_headers X-Auth-User
+            copy_headers X-Auth-User X-Auth-Actor
             @denied status 401 403
             handle_response @denied {
                 redir * /auth/login 302
