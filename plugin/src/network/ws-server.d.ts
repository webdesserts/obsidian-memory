/**
 * Type declarations for the dynamically loaded WebSocket server module.
 * The actual implementation is in WebSocketServer.ts, built separately for Node.js.
 */

import { SyncWebSocketServer } from "./WebSocketServer";

export { SyncWebSocketServer };
